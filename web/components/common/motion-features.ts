// Keep layout animation support for selected resource chips, but fetch the
// animation engine after hydration instead of blocking the initial render.
export { domMax as default } from "framer-motion";
